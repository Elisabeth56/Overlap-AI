{
  "name": "overlap",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:agent": "cd services/agent && uvicorn overlap.main:app --reload --port 8000",
    "seed": "cd services/agent && python -m demo.seed"
  }
}
